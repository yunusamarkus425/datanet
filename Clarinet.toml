[project]
name = 'datanet'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.token-manager]
path = 'contracts/token-manager.clar'
clarity_version = 3
epoch = '3.2'

[contracts.data-marketplace]
path = 'contracts/data-marketplace.clar'
clarity_version = 3
epoch = '3.2'
depends_on = ['token-manager']
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
